# 📚 Online Library Client

Клієнтська частина курсової роботи на тему **"Аналіз методів кешування у веб-застосунках"**.
Це Single Page Application (SPA), розроблений на **React.js**, який демонструє різні стратегії збереження даних на стороні браузера для підвищення продуктивності та покращення UX.

## 🚀 Функціонал та Методи Кешування

У цьому проєкті реалізовано та досліджено такі рівні кешування:

### 1. Персистентне кешування (`localStorage`)
* **Автентифікація:** JWT-токен користувача зберігається після входу. Це дозволяє залишатися в системі навіть після перезавантаження сторінки або закриття браузера.
* **Налаштування:** Обрана тема оформлення (Темна/Світла) зберігається локально.
* **Обране:** Список улюблених книг дублюється в локальному сховищі для офлайн-доступу.

### 2. Сесійне кешування (`sessionStorage`)
* **Фільтри пошуку:** Параметри сортування та активні фільтри каталогу зберігаються в межах сесії вкладки. При оновленні сторінки фільтри не збиваються, але очищаються при закритті вкладки.

### 3. Оперативне кешування (`useState` / React State)
* **Миттєвий відгук:** Введення тексту в пошук та перемикання UI-елементів відбувається миттєво без зайвих запитів до сервера завдяки реактивному стану.

---

## 🛠 Технологічний стек

* **Core:** React.js (Create React App)
* **State Management:** React Hooks (`useState`, `useEffect`, Custom Hooks)
* **Routing:** React Router DOM
* **HTTP Client:** Fetch API / Axios
* **Storage:** Web Storage API, IndexedDB

---

## ⚙️ Як запустити проєкт

1. **Клонуйте репозиторій:**
   ```bash
   git clone <посилання-на-твій-репо>
   npm install 
   npm start
